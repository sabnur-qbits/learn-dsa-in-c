#include<stdio.h>
int main(){
    int a,b,c,p;
    printf(" Enter three numbers :");
    scanf("%d %d %d", &a,&b,&c);
    if( a > b){
        (p == a);
    }
    else if (a<b){
        (p == b);

    }
    else if( p> c){
        printf("The largest number is %d  ", p);
    }
    else{
        printf( " The largest number is %d ", c);
    }
    
    
}
